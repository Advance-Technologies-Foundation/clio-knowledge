# Capabilities

This directory will define stable identifiers for use cases, supporting capabilities, architectural decisions, patterns, and hazards.

Controlled identifiers allow Clio to find examples by primary use case or supporting mechanism without relying on inconsistent free-form tags. Aliases may improve search, but the stable identifier remains authoritative.
