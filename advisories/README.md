# Advisories

This directory will contain high-visibility safety advisories, known-dangerous patterns, limitations, and urgent corrections.

Advisories are independently addressable so Clio can include them whenever an affected capability or workflow is requested. A critical advisory does not replace executable enforcement where Clio can prevent the unsafe operation directly.
