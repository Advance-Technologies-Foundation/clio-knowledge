# Schemas

This directory will contain machine-readable contracts for guidance manifests, advisories, capabilities, reference-example metadata, routing, and published bundles.

Schemas must remain versioned and backward-compatible where practical. A Clio client must be able to reject a bundle schema it cannot understand without losing its last-known-good guidance.
