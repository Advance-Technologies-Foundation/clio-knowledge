# P1 test key

These keys are disposable public test fixtures. The private key is intentionally committed so
the signed P1 conformance bundle can be rebuilt by any contributor. It provides no production
trust and must never be configured as a production Clio knowledge key.
